# Step 1: Use our custom-named development flake
use flake ./dev-flake.nix

# Step 2: Optional - specify which devShell to use
# use flake ./dev-flake.nix#minimal  # for minimal shell
# use flake ./dev-flake.nix#default  # explicit default (same as above)

# Step 3: Watch additional files for changes (optional)
# This makes direnv reload when these files change
watch_file dev-flake.nix
watch_file flake.lock